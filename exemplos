'''condicionais é importante para se fazer comparações entre variaveis ou valores boolwnoa, numericos eu de palavras]
para realizar esses elementos é importante utilizar a condição if seiguida um se não else ou nada '''
 


moeda = True
ponto = 0
nome = str(input('nome do personagem:\n'))
if((nome == "Mario" and moeda == True) or (nome =="mario" and moeda ==True)):
   ponto = ponto + 1
   print(f'{nome} pegou a moeda e teve {ponto}')
   moeda = str(input("deseja pegar mais moedas?\n"))
   if(moeda == "sim"):
     ponto = ponto + 1 
     print(f" {nome} pegou a moeda e teve {ponto}")  
   else:
       print(f"Não quis pegar a moeda")
else:    
    print("Não pegou a moeda")